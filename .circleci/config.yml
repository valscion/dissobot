version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:24.12
    steps:
      - checkout
      - run: npm install
      - run: npm test
      - run: npm run flow:test
      - run: npm run lint

workflows:
  ci:
    jobs:
      - build
